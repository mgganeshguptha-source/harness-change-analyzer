# harness-change-analyzer dependencies.
#
# The analyzer also needs the GitHub Copilot CLI (Node) when reasoning=copilot,
# installed in the workflow via:  npm install -g @github/copilot
# (mock reasoning needs only PyYAML.)

# The Copilot SDK the analyzer drives for reasoning (imports as `copilot`).
# Pinned to the same version the engine uses, to avoid schema drift.
github-copilot-sdk==1.0.4

# YAML parsing (registry, change sets, story frontmatter).
pyyaml>=6.0
